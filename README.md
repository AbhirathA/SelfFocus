# SelfFocus

SelfFocus is an offline Windows desktop app that adds reflective friction to breaks.

## Running locally

1. Create and activate a virtual environment.
2. Install dependencies:
   `pip install -r requirements.txt`
3. Start the app:
   `python -m selffocus.main`

## Packaging

The repo includes a PyInstaller spec file for building a single executable.
